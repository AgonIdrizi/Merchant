<div class="dropdown-menu cart cart-data cart-nav" aria-labelledby="navbarDropdown">
  
  <div class = "cart-data">
  <% @cart.order_items.each do |item| %>
    
    <p class="item-<%=item.id%>">
      <%= item.product.title %>
      
      <td><%= link_to item.quantity, edit_order_item_path(item), class: 'edit-order-item' %></td>
      
      <span ><%= print_price item.subtotal %></span>
      <td><%= link_to (fa_icon "trash"), item, method: :delete ,remote:true %></td>
    </p>
  <% end %>
  </div>
    <div class="dropdown-divider"></div>
    <p >
    Cart Total:<%= print_price @cart.total %></a>
    </p>
</div>